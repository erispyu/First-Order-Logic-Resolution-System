public class ResolutionUtility {
    public static boolean query(SingleLiteral query, KnowledgeBase knowledgeBase) {
        boolean result = false;
        //TODO: actually query
        return result;
    }
}
