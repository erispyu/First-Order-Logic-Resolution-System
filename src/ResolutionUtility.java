import java.util.HashMap;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;

public class ResolutionUtility {

    /**
     *  API for resolution
     * @param c1
     * @param c2
     * @return
     */
    public static CNFClause resolve(CNFClause c1, CNFClause c2) {
        // TODO
        CNFClause resolventClause = new CNFClause("Empty(e)");

        return resolventClause;
    }
}
